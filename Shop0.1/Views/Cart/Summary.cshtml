@model DAL.Entities.Cart

<div id="cart">
    
    @*<span class="caption">
        <b>Your cart:</b>
        @Model.Lines.Sum(x => x.Quantity) item(s),               // item's count and total value/price
        @Model.ComputeTotalValue().ToString("c")
    </span>*@
    <a class="btn btn-success" href="@Url.Action("Index", "Cart",new { returnUrl = Request.Url.PathAndQuery })">Checkout <span class="badge badge-light">@Model.Lines.Sum(x => x.Quantity)</span></a>
    
    
    
    @*@Html.ActionLink("Checkout", "Index", "Cart",
                    new { returnUrl = Request.Url.PathAndQuery },
                    new { @class = "btn btn-outline-success" })*@
</div>