@*попытки*@

<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarTogglerDemo03" aria-controls="navbarTogglerDemo03" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="col">
        <div class="logoimage"><img src="~/pict/logo.png" width="150"></div>
    </div>

    <div class="collapse navbar-collapse text-center" id="navbarTogglerDemo03">

        <div class="col-5">
            <ul class="nav justify-content-center mt-2 mt-lg-0">
                <li class="nav-item active">
                    <a>@Html.ActionLink("HOME", "AllGood", "Good") <span class="sr-only">@Html.ActionLink("HOME", "AllGood", "Good")</span></a>
                </li>
                <li class="nav-item">
                    <a href="#">@Html.ActionLink("CAMERAS", "Cameras", "Good")</a>
                </li>
                <li class="nav-item">
                    <a href="#">LENSES</a>
                </li>
                <li class="nav-item">
                    <a href="#"> CAMCODERS</a>
                </li>

            </ul>
        </div>
    </div>
    <div class="col">
        <button type="button" class="btn btn-success">Register</button>
    </div>
</nav>


@model DAL.Entities.CartLine
@{
    ViewBag.Title = "Cart";
}


<h2>Your cart</h2>
<table style="width:90%;" align="center">
    <thead>
        <tr>
            <th align="right">Quantity</th>
            <th align="left">Item</th>
            <th align="right">Price</th>
            <th align="right">Subtotal</th>
        </tr>
    </thead>
    <tbody>
        @*@foreach (var line in Model.Cart.Lines)
            {*@
        <tr>
            <td align="center">@Model.Quantity</td>
            <td align="left">@Model.Good.GoodName</td>
            <td align="left">@Model.Good.Price.ToString("c")</td>
            <td align="left">@((Model.Quantity * Model.Good.Price).ToString("c"))</td>
        </tr>
        @*}*@
    </tbody>
    @*<tfoot>
            <tr>
                <td colspan="3" align="right">Total:</td>
                <td align="right">
                    @Model.Cart.ComputeTotalValue().ToString("c")
                </td>
            </tr>
        </tfoot>*@
</table>
@*<p align="center" class="actionButtons">
        <a href="@Model.ReturnUrl">Continue shopping</a>
    </p>*@








links which wasnt be necessary
@*<link rel="stylesheet" type="text/css" href="~/Content/bootstrap.min.css">
    <link href="~/Content/bootstrap.css" rel="stylesheet" />
    @*<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
*@

@*<script src="~/Scripts/jquery-3.4.1.min.js"></script>
    <script src="~/Scripts/bootstrap.js"></script>
    <script src="~/Scripts/bootstrap.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js" integrity="sha384-UO2eT0CpHqdSJQ6hJty5KVphtPhzWj9WO1clHTMGa3JDZwrnQq4sF86dIHNDz0W1" crossorigin="anonymous"></script>
    <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js" integrity="sha384-q8i/X+965DzO0rT7abK41JStQIAqVgRVzpbzo5smXKp4YfRvH+8abtTE1Pi6jizo" crossorigin="anonymous"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js" integrity="sha384-JjSmVgyd0p3pXB1rRibZUAYoIIy6OrQ6VrjIEaFf/nJGzIxFDsf4x0xIM+B07jRM" crossorigin="anonymous"></script>*@





@*<h2>Your basket</h2>
    <div class="container">
        <table style="width:90%;" align="center">
            <thead>
                <tr>
                    <th align="center">Quantity</th>
                    <th align="left">Item</th>
                    <th align="right">Price</th>
                    <th align="right">Subtotal</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var line in Model.Cart.Lines)
                {
                    <tr>
                        <td align="center">@line.Quantity</td>
                        <td align="left"> @line.Good.GoodName</td>
                        <td align="right">@line.Good.Price.ToString("c")</td>
                        <td align="right">@((line.Quantity * line.Good.Price).ToString("c"))</td>
                        <td>
                            @using (Html.BeginForm("RemoveFromCart", "Cart"))
                            {
                                @Html.Hidden("GoodId", line.Good.GoodId)
                                @Html.HiddenFor(x => x.ReturnUrl)
                                <input class="actionButtons" type="submit" value="Remove" />
                            }
                        </td>
                    </tr>
                }
            </tbody>
            <tfoot>
                <tr>
                    <td colspan="3" align="right">Total:</td>
                    <td align="right">
                        @Model.Cart.ComputeTotalValue().ToString("c")
                    </td>
                </tr>
            </tfoot>
        </table>
        <p align="center" class="actionButtons">
            <a href="@Model.ReturnUrl">Continue shopping</a>
            @Html.ActionLink("Checkout now", "Checkout")
        </p>

    </div>*@